# Lessons Learned

• Always configure the Domain Controller with a static IP before installing Active Directory.

• Configure DNS before attempting any domain joins.

• DHCP should distribute the Domain Controller as the primary DNS server.

• A clean OU hierarchy simplifies future Group Policy deployment.

• Validate every configuration before moving to the next deployment phase.
